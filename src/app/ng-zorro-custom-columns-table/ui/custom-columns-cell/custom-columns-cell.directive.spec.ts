import { CustomColumnsCellDirective } from './custom-columns-cell.directive';

describe('CustomColumnsCellDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomColumnsCellDirective();
    expect(directive).toBeTruthy();
  });
});
