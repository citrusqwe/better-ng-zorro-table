import { TableCellDefDirective } from './table-cell-def.directive';

describe('TableCellDefDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCellDefDirective();
    expect(directive).toBeTruthy();
  });
});
