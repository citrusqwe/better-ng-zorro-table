import { TableColumnDefDirective } from './table-column-def.directive';

describe('TableColumnDefDirective', () => {
  it('should create an instance', () => {
    const directive = new TableColumnDefDirective();
    expect(directive).toBeTruthy();
  });
});
